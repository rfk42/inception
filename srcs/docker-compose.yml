version: "3.8"

services:
  nginx:
    container_name: nginx
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    image: nginx:rhamini
    ports:
      - "443:443"
    networks:
      - inception
    restart: always

networks:
  inception:
    driver: bridge
